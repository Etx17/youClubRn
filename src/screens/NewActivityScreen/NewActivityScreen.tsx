import { View, Text } from 'react-native'
import React from 'react'

const NewActivityScreen = (clubId: string) => {
  return (
    // Ask for photos
    // Ask for name of activity
    // Ask for description of activity
    // Ask for address of activity if different from club address
    <View>
      <Text>NewActivityScreen</Text>
    </View>
  )
}

export default NewActivityScreen